# Content TODO — highest-value additions

The site is complete with the factual material currently present in this repository. These additions would materially improve recruiter confidence; the interface does not pretend they already exist.

- Supply 2–4 real captures for ResolveOps, Punchline, and one data or ML project. These are the highest-priority gaps after the three featured products.
- For each flagship project, provide one concrete outcome: users reached, sessions completed, latency measured, failure rate reduced, or a similarly defensible number. A measurement method is more useful than a large claim.
- Add public source, live-product, or short demo-video links only when those URLs are ready. Local projects remain labeled as local case studies or MVPs.
- Add a short first-person account for Gathr, VibeGrid, and Murmur: what changed after the first implementation, what failed, and what you would do differently now.
- Replace the external Google Drive resume URL with a same-origin PDF if a stable downloadable file is added to `public/`.
- Add an optional portrait only if an authored image becomes available; the About page deliberately works without one.
- Add Lab entries through `src/data/experiments.ts` when a study is implemented. Do not publish empty experiment placeholders.
